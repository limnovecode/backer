-# app/views/adminboard/legal_sections/_form.html.haml

= form_for([:adminboard, @legal_section]) do |f|
  - if @legal_section.errors.any?
    #error_explanation
      %h2 #{pluralize(@legal_section.errors.count, "error")} prohibited this legal from being saved:
      %ul
        - @legal_section.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :active
    = f.check_box :active
  .actions
    = f.submit "Save"
